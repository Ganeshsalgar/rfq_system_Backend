spring.application.name=rfq-system

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/rfq_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Ganesh@09
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# Scheduler
spring.task.scheduling.pool.size=5

# Logging
logging.level.com.rfq=DEBUG

# JWT
jwt.secret=rfqSystemSecretKey2025BritishAuctionEngineSecureKey1234567890ABCDEF
jwt.expiration=86400000
